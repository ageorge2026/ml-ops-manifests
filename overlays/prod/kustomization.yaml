apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: production

patches:
  - path: patches.yaml

images:
  - name: ghcr.io/YOUR_GITHUB_USERNAME/YOUR_RAG_APP_REPO
    newTag: prod-PLACEHOLDER_SHA
