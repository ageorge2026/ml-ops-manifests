apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-app-deployment
spec:
  replicas: 3  
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret  # ◄── Updated to match your exact secret name
      containers:
        - name: rag-app-container
