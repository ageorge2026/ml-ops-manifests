apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rag-app-qa
  namespace: automation
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/ageorge2026/ml-ops-manifests.git'
    targetRevision: HEAD
    path: overlays/qa/
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: automation
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
